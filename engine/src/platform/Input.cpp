// Input.cpp
// Engine-side key/mouse code translation lives in Window.cpp alongside GLFW.
// This translation unit is reserved for any future input-system state that
// does not require a GLFW dependency (e.g. action-mapping tables).
#include "Input.hpp"
